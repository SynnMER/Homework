#include "Abonents.h"
PhoneBook::PhoneBook(std::string name, int number) {
	_names.push_back(name), _numbers.push_back(number);
}
abonents::abonents(std::string name, int number) {
	_names.push_back(name), _numbers.push_back(number);
}